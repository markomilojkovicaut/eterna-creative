import { redirect } from "next/navigation";

/** Scope estimator consolidated into app cost calculator for now. */
export default function BubbleScopeEstimatorPage() {
  redirect("/tools/app-cost-calculator");
}
